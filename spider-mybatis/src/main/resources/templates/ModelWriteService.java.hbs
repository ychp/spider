package {{package}}.service;

import {{package}}.model.{{modelName}};

/**
* @author yingchengpeng
* @date {{#formatDate currentDate}}{{/formatDate}}
*/
public interface {{modelName}}WriteService {

    /**
     * 创建
     * @param {{modelParam}} 需要创建的数据
     * @return 主键
     */
    Long create({{modelName}} {{modelParam}});

    /**
     * 更新
     * @param {{modelParam}} 需要更新的数据
     * @return 操作结果
     */
    Boolean update({{modelName}} {{modelParam}});

    /**
     * 根据主键id删除数据
     * @param id 主键
     * @return 操作结果
     */
    Boolean delete(Long id);
}