package {{package}}.service;

import {{package}}.bean.query.{{modelName}}Criteria;
import {{package}}.model.{{modelName}};
import com.ychp.common.model.paging.Paging;

/**
* @author yingchengpeng
* @date {{#formatDate currentDate}}{{/formatDate}}
*/
public interface {{modelName}}ReadService {

    /**
     * 根据id查询
     *
     * @param id 主键id
     * @return 查询结果
     */
    {{modelName}} findById(Long id);

    /**
     * 根据条件获取分页
     *
     * @param criteria 条件
     * @return 分页结果
     */
    Paging<{{modelName}}> paging({{modelName}}Criteria criteria);

}